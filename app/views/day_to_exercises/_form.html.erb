<%= form_with(model: day_to_exercise) do |form| %>
  <% if day_to_exercise.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(day_to_exercise.errors.count, "error") %> prohibited this day_to_exercise from being saved:</h2>

      <ul>
        <% day_to_exercise.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :day_id, style: "display: block" %>
    <%= form.text_field :day_id %>
  </div>

  <div>
    <%= form.label :exercise_id, style: "display: block" %>
    <%= form.text_field :exercise_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
